#pragma once

// singleton class for managing the state of a Tcp server/client(s) relationship(s)
static class TcpState
{
public:
	void 

private:
	TcpState () = delete;
	~TcpState () = delete;
	TcpState (const TcpState &) = delete;
	TcpState &operator=(const TcpState &) = delete;
}